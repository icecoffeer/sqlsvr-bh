SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [dbo].[CHKBLPreOrd_100TO1600]
(
  @NUM CHAR(14),
  @OPER CHAR(30),
  @CLS	 CHAR(10),
  @TOSTAT INT,
  @MSG VARCHAR(255) OUTPUT
)
AS
BEGIN
	UPDATE BLPreOrd SET
		STAT = 1600,
		CHKDATE = GETDATE(),
		CHECKER = @OPER,
		LSTUPDTIME = GETDATE()
	WHERE NUM = @NUM
	UPDATE BLPreOrdDTL SET FLAG = 0 WHERE NUM = @NUM
	EXEC BLPreOrdADDLOG @NUM,1600,'从已审核变更回预审',@OPER
	RETURN 0
END
GO
