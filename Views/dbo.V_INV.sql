SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE  VIEW	[dbo].[V_INV]	(
			STORE,
			WRH,
			GDGID,
			QTY,
			TOTAL,
			ORDQTY,
			RSVALCQTY
		) AS
		SELECT	STORE,
			WRH,
			GDGID,
			SUM(QTY),
			SUM(TOTAL),
			SUM(ORDQTY),
			SUM(RSVALCQTY)
		FROM INV
		GROUP BY	WRH, GDGID, STORE
GO
